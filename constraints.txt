# Exact dependency stack this prototype was last verified on
# (2026-08-28, Python 3.12): 32 unit tests green, full zero-key pipeline
# against real yfinance/Binance data, dashboard state matrix green.
#
# Install with:
#   pip install -c constraints.txt -e ./data-layer -e ./feature-engineering -e ./model streamlit
#
# Why pinned: pandas 3.x and streamlit move fast; these exact versions are
# what the test suite and the live pipeline were validated against. Newer
# versions will usually work, but pin to this file until you re-verify.
pandas==3.0.5
numpy==2.2.6
pandas-ta==0.4.71b0
numba==0.61.2
llvmlite==0.44.0
streamlit==1.62.0
pyarrow==25.0.1
altair==6.2.2
yfinance==1.7.0
praw==8.0.3
PyYAML==6.0.3
python-dotenv==1.2.3
requests==2.34.2
scikit-learn==1.9.0
scipy==1.18.1
lightgbm==4.7.0
joblib==1.5.3
